"""
In this we learn about the set in python language.
Set are used {} these braces,and only for empty set we used these braces().
"""
e=set()  #this is denote the empty set
#s={} or s=[] are not denote the empty set and it is not used the empty set, it give the error.
s={1,2,4,5,5,75,75,34,23} 
print(s)
#In set the value are not repeat if value repeat it give repeated value only one time.
#if value are not repeat to we use set
#if the order are matter in data so we used List.